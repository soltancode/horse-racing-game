// Total number of horses available in the game
export const TOTAL_HORSE_COUNT = 20;

// Number of horses selected for each race
export const HORSES_PER_RACE = 10;

// Total number of rounds
export const TOTAL_ROUNDS = 6;

// Round-specific distances (meters)
export const ROUND_DISTANCES = [1200, 1400, 1600, 1800, 2000, 2200];

// Maximum progress value before finish line is reached
export const MAX_PROGRESS = 85;

// Race interval speed in milliseconds
export const RACE_TICK_INTERVAL = 200;

// Maximum progress gain per tick
export const MAX_TICK_INCREMENT = 5;
